# 팀원 모델 연동 가이드 (ONNX 온디바이스)

계획서 "경로 A(권장)" 구현. **서버 없이 브라우저에서 직접 추론**하므로
영상이 기기 밖으로 나가지 않고 추론 비용도 0이다.

## 쓰는 법 — 🚀 시작 탭을 위에서 아래로

`sim.html`을 열면 우측 패널이 **🚀 시작** 탭에서 시작한다. 계획서의 작업 순서가
5단계로 놓여 있고, 각 단계에 지금 상태와 누를 버튼이 같이 있다.

| | 단계 | 버튼 |
|---|---|---|
| 1 | 학습 데이터 만들기 | `🏭 데이터셋 생성` |
| 2 | 모델 학습 | 시뮬레이터 밖 — 각자 PC |
| 3 | **내 모델 검증** | `📁 .onnx 선택` → `▶ 검증 시작` |
| 4 | 안전 시연 | `▶ 자동 작업` + `👤 사람 접근` |
| 5 | 회차 비교 | `📋 회차 기록 열기` |

3번 버튼을 누르면 모드가 자동으로 ONNX로 바뀌고 파일창이 열린다. 세부 설정을 직접
만지려면 **📊 데이터** 탭 → *모델 검증(Model-in-the-Loop)* 으로 가면 된다.

로드에 성공하면 아래처럼 표시된다.

```
✔ best.onnx · 실행 WEBGPU · 입력 640×640 (images) · 출력 output0
```

## 자동으로 처리되는 것

| 항목 | 처리 |
|---|---|
| 실행 백엔드 | WebGPU 우선, 실패 시 wasm 자동 폴백 |
| 입력 해상도 | 모델 메타데이터에서 감지 (동적이면 640) |
| 입출력 이름 | 세션에서 자동 |
| 전처리 | 레터박스(종횡비 유지, 회색 114 패딩) + 0~1 정규화 + NCHW |
| 후처리 | 클래스별 NMS(IoU 0.45) + 레터박스 역변환 + 정규화 좌표 |
| 출력 레이아웃 | `[1,C,N]` / `[1,N,C]` / YOLOv5 objectness 자동 판별 |

## 모델 쪽에서 맞춰야 하는 것

- **opset 12 이상**, 입력 `float32 [1,3,H,W]`, 0~1 정규화 기준
- 출력은 YOLO 계열 (v8: `4+nc`, v5: `5+nc`)

### 클래스 순서가 다르면

기본값은 `["person", "fire", "smoke"]` 이다. 다르면 콘솔에서 한 줄:

```javascript
__sim.ONNX.labels = ["person", "fire", "smoke", "robot"]
```

`person` 이라는 라벨이 있어야 **모델 기반 안전 정지** 시연이 동작한다.

### 임계값 조정

```javascript
__sim.ONNX.conf = 0.25    // 신뢰도 하한
__sim.ONNX.iou  = 0.45    // NMS IoU
__sim.ONNX.layout = "v8"  // auto | v8 | v5 (자동 판별이 틀릴 때만)
```

## 검증 결과 확인

- 화면에 박스가 그려지고, 하단에 **`모델 출력 — N초 전 프레임 기준 · 드리프트 Nm`** 표시
- **드리프트** = 모델이 응답하는 동안 사람이 실제로 이동한 거리.
  이 값이 크면 "모델이 본 위치"와 "실제 위치"가 벌어진다는 뜻이다.
- `모델 기반 안전 정지` 체크 시 로봇이 **모델이 마지막으로 본 위치**로만 판단한다 —
  지연·미검출의 위험을 그대로 보여주는 시연.

## 동작 확인용 더미 모델

`vendor/ort/_test_yolov8_dummy.onnx` — 고정 박스 3개(person 0.92 / person 0.81 / fire 0.55)를
반환하는 YOLOv8 형태 모델. **배선이 살아 있는지 먼저 확인**할 때 쓴다.

실측(2026-08-13): WebGPU 백엔드, 추론 **12~100ms**, 박스 3개 정상 디코드·오버레이 표시.

## 회차 기록 — 내 모델이 지난번보다 나아졌나

`▶ 검증 시작` ~ `■ 검증 중지` 사이가 **1회차**로 자동 기록된다 (📊 데이터 탭 → *회차 기록*).
브라우저에 남으므로 새로고침해도 유지된다. 비교하려면 **회차 라벨**에 모델명을 적어 둔다
(`yolov8n_v3` 처럼). 같은 라벨의 직전 회차 대비 증감이 화살표로 뜬다 — 초록이 개선.

| 지표 | 뜻 |
|---|---|
| `latMean` / `latP95` | 추론+반영 지연. p95는 최악에 가까운 쪽 |
| `detRate` | 응답 중 `person` 박스가 있던 비율 |
| `driftMax` | 모델이 응답하는 동안 사람이 이동한 최대 거리 |
| **`riskPct`** | **실제로는 정지 반경 안인데 로봇이 안 선 프레임 비율.** 낮을수록 안전 |
| `blindPct` | 실제 사람이 감속 반경 안인데 모델은 '없음'으로 본 프레임 비율 (미검출·가림) |
| `preSlow` / `leadMean` | 선제 감속 발동 횟수 / 그때부터 실제 정지까지 벌어 둔 여유 거리 |
| `stops` / `estop` | 안전 정지 / 접촉 E-STOP 횟수 |

`⬇ CSV` 로 내보내면 회차가 행으로 쌓여 그대로 표·그래프로 쓸 수 있다.
콘솔에서는 `__sim.RUNLOG.runs` 로 원본 JSON을 본다.

**비교할 때 주의** — 회차마다 `randomizeScene()`이 장면을 바꾸므로 차이에는 장면 변동이 섞인다.
`meta`에 카메라·주기·지연·인원·사고시나리오가 함께 기록되니, **조건이 같은 회차끼리** 비교한다.

### 알려진 한계 — 먼 접근 구간은 카메라 사각이다

실측(2026-08-13): 로봇을 향한 접근 경로에서 사람이 **1.41m부터 검출**된다. 그 바깥
(1.85m 이상)은 CCTV 3대 어디에도 안 잡힌다. 안전 정지(1.2m)는 정상 동작하지만,
**예측 기반 선제 감속은 모델 기반 모드에서 발동하지 못한다** — 발동 지점(~2.1m)에서
사람이 보이지 않기 때문이다. 안전존 자체의 커버리지는 정지링 100% / 감속링 81%.

이는 카메라 3대 배치의 실제 성질이며, 회차 기록의 `blindPct`가 이 값을 정량화한다.

## 데이터셋 클래스가 6종으로 바뀌었다 (2026-08-13)

```
0 person   1 fire   2 smoke   3 robot   4 kettle   5 equipment
```

**계획서가 요구하는 person·fire·smoke를 0·1·2로 앞에 뒀다.** 종전에는 fire·smoke가
아예 없어서, 이 시뮬로 만든 데이터로 학습하면 **불과 연기를 배울 수 없었다**
(모델은 3종을 찾아야 하는데 라벨은 person 하나뿐이었다).

> **id가 바뀌었으므로 이전에 생성한 데이터셋과 섞어 쓰면 안 된다.** 새로 생성할 것.

불·연기는 파티클이다. 마스크 패스에서 파티클을 끄지 않고, 살아 있는 입자의 색을
클래스 색으로 덮어쓴 뒤 **불투명 단색**으로 찍는다.
→ **이미지에 보이는 그 픽셀이 그대로 라벨**이 된다. 라벨과 이미지가 따로 놀 수 없다.

- 불이 꺼져 있으면 라벨 0개 (실측 확인)
- 입자가 적은 프레임에서는 박스도 작게 나온다 — 의도된 동작이다
- 깊이맵에는 파티클을 넣지 않는다 (거리값 오염 방지)

> 처음에는 부피를 대신하는 프록시 메시로 만들었다가 폐기했다. 라벨은 항상 있는
> 프록시에서, 이미지는 매번 달라지는 파티클에서 나오니 **둘이 따로 놀았다** —
> 이미지에 연기가 없는데 라벨만 남는 프레임이 생겼다(박스를 이미지에 얹어 확인).

## 궤적 예측 모델도 꽂을 수 있다

계획서의 예측 3단계는 `등속 → 칼만 → 학습형`이다. 앞의 둘은 시뮬레이터가 직접 푼다.
**학습형은 팀원 모델**이므로 꽂을 자리만 만들어 뒀다 — 검출기와 같은 방식이다.

📊 데이터 탭 → *궤적 예측* → `학습형` 선택 후, 콘솔에서 함수 하나를 등록한다.

```javascript
window.__customPredictor = async (obs) => {
  // obs = { now, pos:{x,z}, vel:{x,z}, hist:[{t,x,z},…], horizon, mPerAU }
  //   pos·vel·hist : 씬 좌표(AU) — 미터로 바꾸려면 mPerAU를 곱한다
  //   hist         : 최근 2초 관측, 오래된 것부터
  return {
    path: [{t:0, x:…, z:…}, …, {t:obs.horizon, x:…, z:…}],  // t = 지금부터 초
    sigma: 0.2,   // 지평선 끝 위치 불확실성 1σ (m)
  };
};
```

| 항목 | 동작 |
|---|---|
| 호출 주기 | 10Hz (`__sim.CPRED.hz`로 변경) |
| 응답 대기 중 | 직전 경로를 계속 사용 — 실제 온디바이스 추론과 같은 조건 |
| **1초 넘게 응답 없음** | **칼만으로 자동 대체** (멈춘 모델의 옛 경로로 판정하면 위험) |
| 예외 발생 | 칼만으로 대체 + 패널에 실패 횟수 표시 |
| `sigma` 생략 | 0으로 간주 — **보수적이지 않으니 반드시 채울 것** |

`sigma`는 안전 판정에 직접 들어간다. 지평선 끝에서 `sigma`만큼 **더 가까이 있을 수 있다**고
보고 정지/감속 시점을 앞당긴다. 칼만은 이 값을 스스로 내지만(실측 σ 0.47m), 학습형은
모델이 직접 내야 한다.

## 치수는 아직 미검증이다 (±25%)

씬은 **"튀김솥 외경 = 1.25 m"** 가정 위에 서 있다. 이 값은 실측이 아니라 조리원
어깨너비 대비 추정이다. 배치·비례·가림 관계·카메라 각도는 이 값과 무관하게 옳지만,
**미터로 인용하는 값**(로봇 도달거리, 안전거리, GT 깊이)은 ±25% 오차를 안고 있다.

실측 1건이 생기면 지오메트리를 다시 만들 필요 없이 파라미터 하나로 전체가 재보정된다.

```
sim.html?fryer=1.18
```

이때 데이터셋 `gt/*_meta.json` 의 `scale.verified` 가 `true`로 바뀌고 깊이 범위도 함께
환산된다. 재는 방법은 `analysis/measurement_request.md`.

## 런타임 파일

`vendor/ort/` — onnxruntime-web 1.20.1 (WebGPU JSEP 포함).
프로젝트에 동봉돼 있어 **실행 중 외부 네트워크 접근이 없다**.

```
ort.webgpu.min.js                  326KB
ort-wasm-simd-threaded.jsep.wasm    21MB   (WebGPU/JSEP)
ort-wasm-simd-threaded.wasm         11MB   (wasm 폴백)
```

## 알려진 제약

- `wasmPaths`는 **절대 URL**이어야 한다 (상대경로면 ES 모듈 해석 실패).
  `file://` 더블클릭이 아니라 `python -m http.server`로 띄워야 동작한다.
- 스레드 수는 1로 고정 — COOP/COEP 헤더 없이 동작시키기 위함.
  성능이 부족하면 서버에 해당 헤더를 추가하고 `__sim.ONNX.ort.env.wasm.numThreads` 를 올린다.

---

## 팀원에게 보낼 것 — 최소 배포 세트 (51MB)

아래만 복사해서 압축하면 각자 로컬에서 자기 모델로 돌려볼 수 있다.
**실측 검증(2026-08-13)**: 깨끗한 폴더에 아래만 복사해 다른 포트로 띄웠을 때
메시 1027개·설비 21개·사람·물리·로봇 도달성 정상, ONNX 추론 WebGPU 79.5ms·박스 3개, 콘솔 에러 0.

```
START_HERE.md  MODEL_HANDOFF.md             ← 설명서. 빠뜨리면 팀원이 아무것도 모른다
sim.html  index.html                        264KB
babylon.js  babylonjs.loaders.min.js        8.3MB
HavokPhysics_umd.js  HavokPhysics.wasm.js   2.8MB
assets/   (실제 로드되는 GLB 20개만)          6.5MB
vendor/ort/                                  32MB
```

`assets/` 는 sim.html이 실제로 부르는 20개만 넣는다. 목록:

```
basket  kitchen_static  lid  cook_rig_v47
robot_j1  robot_upper  robot_fore  robot_hand  robot_pedestal
env_table  env_rack  env_sink  env_bin  env_pancart  env_wallrack
env_kettle_nb  env_kettle_nb_closed  env_fridge  env_serve  env_basketcart
```

`assets/_legacy`, `cook_cc_v1.glb`, `sketchfab_cook_raw.glb` 등 **작업 부산물 220MB는 제외**한다.

> **`refs/` 는 배포하지 않는다.** 실제 급식실 CCTV 프레임이고 조리원이 찍혀 있다.
> 참조 오버레이(`sim.html?ref=…`)는 이 폴더가 없으면 조용히 꺼지며, 시뮬레이터 동작에는
> 영향이 없다. 같은 이유로 `analysis/참조 현장NDEX_INTERNAL.md`(학교명 대응표)도 제외한다.

### 실행

```bash
python -m http.server 5173
```
→ `http://localhost:5173/sim.html`

### 반드시 지켜야 할 것

| | 이유 |
|---|---|
| **`file://` 더블클릭 금지** | ORT가 wasm/mjs를 ES 모듈로 로드해 실패한다. 반드시 http 서버로 |
| **WebGPU 지원 브라우저** | Chrome/Edge 113+ 권장. 없으면 wasm으로 자동 폴백(느림) |
| **런타임 네트워크 불필요** | 외부 CDN 참조 0건 — 인터넷 없이 동작 |
