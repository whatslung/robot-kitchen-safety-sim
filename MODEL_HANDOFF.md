# 팀원 모델 연동 가이드 (ONNX 온디바이스)

계획서 "경로 A(권장)" 구현. **서버 없이 브라우저에서 직접 추론**하므로
영상이 기기 밖으로 나가지 않고 추론 비용도 0이다.

## 쓰는 법 — 🚀 시작 탭을 위에서 아래로

`sim.html`을 열면 우측 패널이 **🚀 시작** 탭에서 시작한다. 계획서의 작업 순서가
5단계로 놓여 있고, 각 단계에 지금 상태와 누를 버튼이 같이 있다.

| | 단계 | 버튼 |
|---|---|---|
| 1 | 학습 데이터 만들기 | `🏭 데이터셋 생성` |
| 2 | 모델 학습 | 시뮬레이터 밖 — 각자 PC |
| 3 | **내 모델 검증** | `📁 .onnx 선택` → `▶ 검증 시작` |
| 4 | 안전 시연 | `▶ 자동 작업` + `👤 사람 접근` |
| 5 | 회차 비교 | `📋 회차 기록 열기` |

3번 버튼을 누르면 모드가 자동으로 ONNX로 바뀌고 파일창이 열린다. 세부 설정을 직접
만지려면 **📊 데이터** 탭 → *모델 검증(Model-in-the-Loop)* 으로 가면 된다.

로드에 성공하면 아래처럼 표시된다.

```
✔ best.onnx · 실행 WEBGPU · 입력 640×640 (images) · 출력 output0
```

## 자동으로 처리되는 것

| 항목 | 처리 |
|---|---|
| 실행 백엔드 | WebGPU 우선, 실패 시 wasm 자동 폴백 |
| 입력 해상도 | 모델 메타데이터에서 감지 (동적이면 640) |
| 입출력 이름 | 세션에서 자동 |
| 전처리 | 레터박스(종횡비 유지, 회색 114 패딩) + 0~1 정규화 + NCHW |
| 후처리 | 클래스별 NMS(IoU 0.45) + 레터박스 역변환 + 정규화 좌표 |
| 출력 레이아웃 | `[1,C,N]` / `[1,N,C]` / YOLOv5 objectness 자동 판별 |

## 모델 쪽에서 맞춰야 하는 것

- **opset 12 이상**, 입력 `float32 [1,3,H,W]`, 0~1 정규화 기준
- 출력은 YOLO 계열 (v8: `4+nc`, v5: `5+nc`)

### 클래스 순서가 다르면

기본값은 `["person", "fire", "smoke"]` 이다. 다르면 콘솔에서 한 줄:

```javascript
__sim.ONNX.labels = ["person", "fire", "smoke", "robot"]
```

`person` 이라는 라벨이 있어야 **모델 기반 안전 정지** 시연이 동작한다.

### 임계값 조정

```javascript
__sim.ONNX.conf = 0.25    // 신뢰도 하한
__sim.ONNX.iou  = 0.45    // NMS IoU
__sim.ONNX.layout = "v8"  // auto | v8 | v5 (자동 판별이 틀릴 때만)
```

## 검증 결과 확인

- 화면에 박스가 그려지고, 하단에 **`모델 출력 — N초 전 프레임 기준 · 드리프트 Nm`** 표시
- **드리프트** = 모델이 응답하는 동안 사람이 실제로 이동한 거리.
  이 값이 크면 "모델이 본 위치"와 "실제 위치"가 벌어진다는 뜻이다.
- `모델 기반 안전 정지` 체크 시 로봇이 **모델이 마지막으로 본 위치**로만 판단한다 —
  지연·미검출의 위험을 그대로 보여주는 시연.

## 동작 확인용 더미 모델

`vendor/ort/_test_yolov8_dummy.onnx` — 고정 박스 3개(person 0.92 / person 0.81 / fire 0.55)를
반환하는 YOLOv8 형태 모델. **배선이 살아 있는지 먼저 확인**할 때 쓴다.

실측(2026-08-13): WebGPU 백엔드, 추론 **12~100ms**, 박스 3개 정상 디코드·오버레이 표시.

## 회차 기록 — 내 모델이 지난번보다 나아졌나

`▶ 검증 시작` ~ `■ 검증 중지` 사이가 **1회차**로 자동 기록된다 (📊 데이터 탭 → *회차 기록*).
브라우저에 남으므로 새로고침해도 유지된다. 비교하려면 **회차 라벨**에 모델명을 적어 둔다
(`yolov8n_v3` 처럼). 같은 라벨의 직전 회차 대비 증감이 화살표로 뜬다 — 초록이 개선.

| 지표 | 뜻 |
|---|---|
| `latMean` / `latP95` | 추론+반영 지연. p95는 최악에 가까운 쪽 |
| `detRate` | 응답 중 `person` 박스가 있던 비율 |
| `driftMax` | 모델이 응답하는 동안 사람이 이동한 최대 거리 |
| **`riskPct`** | **실제로는 정지 반경 안인데 로봇이 안 선 프레임 비율.** 낮을수록 안전 |
| `blindPct` | 실제 사람이 감속 반경 안인데 모델은 '없음'으로 본 프레임 비율 (미검출·가림) |
| `preSlow` / `leadMean` | 선제 감속 발동 횟수 / 그때부터 실제 정지까지 벌어 둔 여유 거리 |
| `stops` / `estop` | 안전 정지 / 접촉 E-STOP 횟수 |

`⬇ CSV` 로 내보내면 회차가 행으로 쌓여 그대로 표·그래프로 쓸 수 있다.
콘솔에서는 `__sim.RUNLOG.runs` 로 원본 JSON을 본다.

**비교할 때 주의** — 회차마다 `randomizeScene()`이 장면을 바꾸므로 차이에는 장면 변동이 섞인다.
`meta`에 카메라·주기·지연·인원·사고시나리오가 함께 기록되니, **조건이 같은 회차끼리** 비교한다.

### 알려진 한계 — 먼 접근 구간은 카메라 사각이다 (기본 배치 한정)

실측(2026-08-13): 로봇을 향한 접근 경로에서 사람이 **1.41m부터 검출**된다. 그 바깥
(1.85m 이상)은 CCTV 3대 어디에도 안 잡힌다. 안전 정지(1.2m)는 정상 동작하지만,
**예측 기반 선제 감속은 모델 기반 모드에서 발동하지 못한다** — 발동 지점(~2.1m)에서
사람이 보이지 않기 때문이다. 안전존 자체의 커버리지는 정지링 100% / 감속링 81%.

이는 카메라 3대 배치의 실제 성질이며, 회차 기록의 `blindPct`가 이 값을 정량화한다.

> **이 한계는 기본 배치(`sim.html`을 그냥 열었을 때) 이야기다.**
> 아래 섬 배치는 성질이 다르다 — 벽 8대 + 특수 5대가 **안전존 사각 0칸 · 3대 이상 중첩 100%**,
> 정지링 진입로도 벽 8대만으로 93%를 덮는다.

## 카메라 구성 — 섬 배치 (2026-08-18 개정)

데이터셋은 **`sim.html?layout=island`** 에서 뽑는다. 방 11.5×11.5m · 천장 3.9m 기준이다.

### 왜 원형 링을 버렸나

종전에는 솥을 반경 3.754m로 둘러싼 **원형 링 10대**가 전부 원점을 겨눴다. 그 결과
**10장이 전부 같은 구도**가 됐다 — 솥이 어느 화면에서나 좌표 (0.00, 0.21)에 왔다(실측).
방위별로 배경만 다르고 피사체 배치는 동일해서, 학습 데이터로는 '같은 그림 10장'에 가까웠다.
게다가 벽에서 평균 2.37m 떨어진 허공에 매달려 있어 실제 급식실에 존재할 수 없는 설치였다.

지금은 **실제 CCTV처럼 벽·모서리에 붙이고 카메라마다 담당 구역을 따로 준다.** 규칙으로
좌표를 만들지 않는다 — 한 대는 입구, 한 대는 배식구, 한 대는 조리라인처럼 목적별로
하나씩 앉힌다. **조준점이 서로 다른 것이 이 배치의 핵심**이고, 그래서 구도가 갈린다.

| | 링 10대 | **구역담당 8 + 특수 5** |
|---|---|---|
| 안전존 사각 | 1칸 | **0칸** |
| 안전존 3대 이상 중첩 | 96.2% | **100%** |
| 사람 작업 지점 21곳 3대 이상 | 17/21 | **21/21** |
| 걸을 수 있는 바닥 1대 이상 | 94.0% | **99.3%** |
| 구도 분산(0 = 전부 동일) | 0.00 | **0.14** |
| 총 대수 | 14 | **13** |

**대수를 줄이면서 커버리지가 올랐다.** 벽에서 보면 한 대가 담는 바닥이 넓어서다.

### 이름이 두 벌이다

- **목록 이름** — 좌측 STAGE 트리. 이름 자리가 115px뿐이라 짧게 줄인 쪽
- **전체 이름** — 📷 카메라 탭 드롭다운, 그리고 목록에 마우스를 올렸을 때 뜨는 툴팁

### 구역 담당 8대 — 학습 데이터의 본체

**벽 안쪽 0.35m · 높이 3.55m**(천장 아래 0.35m) — 벽·천장 접합부에 브래킷으로 붙이는
실제 설치 자리다. 조준 거리 3.7~4.9m · 하향각 36~44°로 실사 급식실 CCTV 범위(30~45°) 안에 든다.

| 목록 | 전체 이름 | id | 좌표 (x, y, z) | 조준(바닥) | 하향 |
|---|---|---|---|---|---|
| 북서 | CAM1 북서 모서리 · 전처리 북 | `cvNW` | −5.40, 3.55, −5.40 | −2.4, −1.6 | 36° |
| 조리라인 | CAM2 북벽 · 조리라인 앞 | `cvN` | 0, 3.55, −5.40 | 0.4, −1.0 | 39° |
| 북동 | CAM3 북동 모서리 · 세척 북 | `cvNE` | 5.40, 3.55, −5.40 | 2.6, −1.4 | 36° |
| 통로 | CAM4 동벽 · 통로·제어반 | `cvE` | 5.40, 3.55, 0 | 1.2, 0.8 | 40° |
| 입구 | CAM5 남동 모서리 · 입구·건조선반 | `cvSE` | 5.40, 3.55, 5.40 | 2.4, 2.0 | 38° |
| 로봇 남 | CAM6 남벽 · 로봇 셀 남측 | `cvS` | 0, 3.55, 5.40 | −0.4, 1.4 | 41° |
| 배식구 | CAM7 남서 모서리 · 배식구 | `cvSW` | −5.40, 3.55, 5.40 | −2.6, 2.2 | 40° |
| 로봇 서 | CAM8 서벽 · 로봇 셀 서측 | `cvW` | −5.40, 3.55, 0 | −1.8, −0.6 | 44° |

> **조준점을 같게 만들면 안 된다.** 대칭 배치에 균일한 조준을 주면 8대가 전부 같은 각도로
> 같은 걸 보게 되어 원형 링과 똑같아진다(실측: 조준을 35° 일괄로 틀어도 구도 분산 0).

### 특수 5대

| 목록 | 전체 이름 | id | 좌표 | 하향 | 역할 |
|---|---|---|---|---|---|
| 해치 정면 | CAM9 솥 해치 정면 | `cvHatch` | 0, 1.50, −2.88 | 12° | 솥 개구부를 정면으로 |
| 판 | CAM10 뚜껑 뒤 판 · 통로 접근 | `cvPole` | 0.86, 1.55, 0.30 | 16° | 근접 저각 광각 |
| 저상 | CAM11 저상 · 쓰러진 사람용 | `lowFloor` | −5.35, 1.60, 3.60 | 6° | **누운 사람의 옆모습 전신** |
| 불꽃 | CAM12 솥 안 · 불꽃 | `fireSE` | 2.17, 2.05, 1.25 | 22° | 후드 밑에서 솥 안 |
| 불꽃 2 | CAM13 솥 안 · 불꽃(반대편) | `fireNW` | −1.40, 2.05, −2.42 | 20° | 같은 목적, 반대 방위 |

**해치 정면**(`cvHatch`) — 나머지가 전부 12~44°로 내려다봐서 *솥 안에서 무엇이 올라오는지*와
*손이 개구부로 들어가는 장면*을 옆에서 못 본다. 북측 준비대 상판(1.02m)에서 0.48m 브래킷.
로봇 베이스에서 3.83m로 팔 반경(1.81m) 밖이다.
처음에 솥 북측 1.20m 허공에 뒀다가 물렸다 — 붙일 데가 없었고, 솥이 화면 가로의 56%를 먹어
CCTV가 아니라 제품 사진처럼 보였다. 2.85m로 물리니 25%로 내려가고 불꽃 표본은 75 → 79%로 올랐다.

**판**(`cvPole`) — 뚜껑 뒤 기둥판(`PillarLCap` 상판 1.12m)에서 0.43m 브래킷.
판은 z −0.37~0.34로 **세운 뚜껑 폭(z ±0.65) 안에 들어 있어**, 통로 쪽 끝(z +0.30)으로
당겨야 뚜껑 옆을 지나 통로가 보인다(북단 19~21% → 남단 **34%**).
조준을 솥이 아니라 통로 쪽으로 튼 것과 렌즈만 2.0mm로 넓힌 것도 같은 이유다.

**불꽃 2대** — 불꽃은 유면(y 0.77)에서 피어올라 후드(바닥 2.20m) 아래 갇히고 연기만 위로
빠진다. **머리 위 카메라는 구조적으로 불꽃을 못 본다.**

### 렌즈 — 3.0mm 공통, `cvPole`만 2.0mm

세로 **65.9°** · 가로 **81.7°** (1/2.8" 4:3 센서, 데이터셋 960×720 기준).
카메라마다 화각이 다르면 같은 사람이 시점마다 다른 크기로 찍혀 학습이 흔들리므로 통일한다.

**`cvPole`만 2.0mm(가로 105°)로 예외**다. 솥 바로 옆 1.55m에서 통로를 보는 근접 카메라라
3.0mm로는 정지링 진입로가 34%에서 더 안 나온다. 회차 실험은 `?lens=3.6` 처럼 연다.

### 이 구성의 실측 성적

사람 1.60m · **몸통(0.50h)·어깨(0.82h)·머리(0.94h) 3점** 기준. 몸통 한 점만 재면
키를 낮췄을 때 사각이 잘못 잡힌다(실측으로 확인).

| 항목 | 값 |
|---|---|
| 안전존(로봇 2m) 사각 | **0칸** |
| 안전존 3대 이상 중첩 | **100%** |
| 사람 작업 지점 21곳 사각 | **0곳** (3대 이상도 21/21) |
| 걸을 수 있는 바닥 1대 이상 | **99.3%** (3대 이상 98.6%) |
| 정지링 진입로(2.6~3.9m) | 벽 8대만으로 93% (3대 이상 89%) |
| 불꽃 표본 — fireSE·fireNW·해치 합집합 | **100%** (뚜껑 각도와 무관) |

> **id가 전부 바뀌었다.** `z8SE`/`zr023` → `cvNW`·`cvHatch` 형식.
> 데이터셋 파일명·`meta` 키가 이 id를 쓰므로 **2026-08-18 이전 회차와 섞으면 안 된다.**

## 안전링 — 2개 → **3개(신호등)** · 값도 재산정 (2026-08-18)

### 링이 3개인 이유

종전에는 정지·감속 2개뿐이었고 정지링이 노랑이었다. 그러면 **"노란 선 밖 = 안전"**으로
읽힌다. 그런데 정지링은 *로봇을 세우라고 명령하는 선*이지 *팔이 닿는 선*이 아니다.
팔이 닿는 선을 따로 그리지 않으면 그림이 사실을 잘못 말한다.

| 색 | 반경 | 뜻 | 무엇이 정하나 |
|---|---|---|---|
| 🟢 초록 | **3.90m** | 감속 경계 — 밖은 안전 | ISO 계산 (정지 + v_h×0.5s) |
| 🟠 주황 | **3.10m** | 정지 명령 — 안은 주의 | ISO 계산 (팔 반경 + Sp) |
| 🔴 빨강 | **1.87m** | **팔이 닿는다** — 안은 위험 | **팔 길이** (upper+fore+grip) |

**빨간 링은 `?reach=`를 따라간다.** `LAYOUT.robot`에서 계산하므로 팔을 키우면 같이 커진다.
숫자로 박아 두면 팔만 커지고 링은 그대로 남는다. 정지·감속은 ISO 계산값이라 팔 길이와
무관하게 고정이고, 빨간 링은 순수 물리값이라 `ringMode`(fixed/ssm)와도 무관하게 항상 같다.

> **정지 시 색 규약** — 종전에는 정지가 걸리면 노란 링이 빨강으로 바뀌었다. 이제 빨강은
> '팔이 닿는 반경'이라는 고정된 뜻을 가지므로, 정지 시에는 **주황이 더 밝은 주황으로**
> 올라간다(`#e3853d` → `#ff7b1a`). 색이 뜻을 배신하면 안 된다.

**이 그림이 곧 이 연구의 논점이다.** 빨간 링이 주황 링 *안쪽*에 있다는 것은, 로봇을 세우라고
명령하는 시점에 사람이 이미 팔 반경 밖 1.23m까지 와 있어야 한다는 뜻이다. 그 1.23m가
**검출 지연 + 정지 시간**이고, 검출이 느려지면 주황 링이 밖으로 밀린다. 빨간 링은 그대로다.

### 값 재산정

종전 1.2/2.0은 어떤 계산에서도 나오지 않는 값이었다. **정지링(1.2m)이 팔이 실제로 휘두르는
반경(1.81m)보다 작아서, 링 밖에 서 있어도 팔에 맞았다.** 물리적으로 모순이다.

ISO/TS 15066 SSM: `Sp = v_h·(T_r+T_s) + v_r·T_r + v_r²/(2a_s) + C`

| 항목 | 값 | 근거 |
|---|---|---|
| 팔 끝 반경(= 빨간 링) | **1.87m** | upper + fore + grip. 팔이 닿을 수 있는 최대 |
| 팔 작업 반경 | **1.81m** | 7개 작업 지점 IK를 풀어 팔 메시 최대 수평거리 실측 |
| v_h 사람 보행 | 1.6 m/s | ISO |
| T_r 반응시간 | 0.35 s | 카메라 0.1 + 온디바이스 추론 0.15 + 제어 0.1 |
| v_r TCP 속도 | 0.5 m/s | 아래 참조 |
| a_s 감속 | 2.5 m/s² | 협동로봇 보수값 |
| C 침입 여유 | 0.15 m | ISO 13855 |
| → Sp | **1.26m** | |
| → **정지링** | **3.10m** | 1.81 + 1.26 (안전측 올림) |
| → **감속링** | **3.90m** | 정지 + v_h × 0.5s |

**로봇 TCP 속도도 같이 낮췄다.** 사이클에서 가장 빠른 구간이 '카트로 이송 — 통로 횡단'
1.13 m/s였는데, **그 구간이 팔이 사람 다니는 바닥 위를 지나는 유일한 구간**이다. 실제 협동
운전대로 0.50 m/s로 낮췄다(`SEQ_FRY` ms 1000 → 2260). **속도와 정지링은 같이 움직인다** —
속도를 되돌리면 정지링을 3.89m로 올려야 한다.

발표에 쓸 감도 분석 — **지배 항이 사람 이동(0.88m)이고 그 절반이 검출 지연이다:**

| 검출 지연 T_r | 정지링 |
|---|---|
| 0.50s | 3.38m |
| **0.35s (현재)** | **3.07m** |
| 0.25s | 2.86m |
| 0.15s | 2.65m |

> 부수 효과: 스테이션 21곳의 위험도 분포가 정지 2/감속 3/밖 16 → **정지 7/감속 4/밖 10**으로
> 바뀐다. 양성 표본이 늘어 데이터셋 균형이 좋아진다.

## 사람 신장 — 조리종사자 기준으로 (2026-08-18)

| | 종전 | **현재** |
|---|---|---|
| 주인공 | 1.72m | **1.60m** |
| 추가 인원 | 1.55~1.85m 랜덤 | **1.50~1.75m 랜덤** |
| 어깨폭 | 0.94~1.10배 랜덤 | 그대로 |

1.72m는 일반 성인 평균(남 174.5 · 여 161.8)의 중간이라 남성 쪽에 치우친 값이었다. 이 시뮬의
대상은 **학교 급식실 조리종사자**다 — 여성이 절대다수(한 조사 99%)이고 40~50대가 대부분이라
평균 신장이 160cm 안팎이다.

**스케일 오류 수정이 아니라 대상 인구를 맞춘 것**이다. 씬의 실물 기준 3개(로봇 도달 1.70m =
두산 H2017 · 솥 림 0.905m = 실물 국솥 850~920mm · 안전링 = ISO)는 전부 그대로 두고 사람만
바꿨다. 오브젝트를 키워 상대 비율을 맞추면 안 된다 — 안전링·카메라 높이·렌즈 화각이 절대
미터에 묶여 있어 씬을 키우면 그 의미가 깨진다.

sim2real 관점: 시뮬에서 큰 사람만 학습하면 실제 160cm 조리원을 더 작게 보고 놓친다.
검출 성능이 실제보다 낙관적으로 나오는 편향이 데이터 단계에서 들어간다.

신장은 `fitPersonHeight()`가 **머리 본의 월드 높이**를 재서 배율을 계산해 홀더에 균등 스케일로
건다. 바운딩박스를 쓰지 않는 이유는 스킨드 메시 bbox가 바인드 포즈 기준이라 실제와 다르기
때문이다(이 함정에 한 번 걸려 1.87m로 잘못 읽은 적이 있다).

## 솥 뚜껑 — 힌지에서 80° 세움 (2026-08-18)

GLB에는 뚜껑 두 짝이 **수평으로 겹쳐 솥 밖(x 0.64~1.32)으로 뻗은 채** 구워져 있었다. 힌지에
매달린 판이 옆으로 누운 그림이라 화면에서 열린 뚜껑이 아니라 회색 판때기로 보였다.

힌지 축(x 0.66 · y 0.94 · z방향) 기준으로 움직이는 부품만 묶어 **80° 들어 올린다**
(x 1.32 → **0.73** · y 0.92~1.60). 힌지 로드·베이스는 솥 고정 부품이라 같이 돌리지 않는다.

각도별 대가는 불꽃 카메라다(림 위 불꽃 표본 24점):

| 뚜껑각 | fireSE | fireNW | 해치 | **합집합** |
|---|---|---|---|---|
| 0° (원래) | 83% | 79% | 79% | **100%** |
| 45° | 75% | 79% | 79% | **100%** |
| **80°** | 33% | 79% | 79% | **100%** |

**해치 카메라가 fireSE의 손실을 메워 합집합은 어느 각도에서도 100%**다. 잃는 건 남동 쪽
이중화 여유지 커버리지가 아니다.

## 아일랜드 간격 (2026-08-18)

뚜껑이 당겨지며 생긴 0.23m를 한쪽에 몰지 않고 두 틈에 고르게 나눴다.

| | 전 | 후 |
|---|---|---|
| 뚜껑 끝 → 국솥1 | 0.15m | **0.25m** |
| 국솥1 → 국솥2 | 0.17m | **0.25m** |
| 국솥2 → 후드 끝 | 0.05m | 0.10m |
| 국솥 중심 | 2.00 / 3.35 | **1.87 / 3.30** |

**한계는 뚜껑이 아니라 후드다**(x −1.60~4.05). 쓸 수 있는 폭이 1.09~4.05 = 2.96m인데 국솥
몸통만 2.36m를 먹는다. 0.60m씩 벌리려면 후드를 늘려야 하고, 그건 카메라 시선에 영향을
주므로 따로 재보고 결정할 일이다.

## 데이터셋 클래스가 6종으로 바뀌었다 (2026-08-13)

```
0 person   1 fire   2 smoke   3 robot   4 kettle   5 equipment
```

**계획서가 요구하는 person·fire·smoke를 0·1·2로 앞에 뒀다.** 종전에는 fire·smoke가
아예 없어서, 이 시뮬로 만든 데이터로 학습하면 **불과 연기를 배울 수 없었다**
(모델은 3종을 찾아야 하는데 라벨은 person 하나뿐이었다).

> **id가 바뀌었으므로 이전에 생성한 데이터셋과 섞어 쓰면 안 된다.** 새로 생성할 것.

불·연기는 파티클이다. 마스크 패스에서 파티클을 끄지 않고, 살아 있는 입자의 색을
클래스 색으로 덮어쓴 뒤 **불투명 단색**으로 찍는다.
→ **이미지에 보이는 그 픽셀이 그대로 라벨**이 된다. 라벨과 이미지가 따로 놀 수 없다.

- 불이 꺼져 있으면 라벨 0개 (실측 확인)
- 입자가 적은 프레임에서는 박스도 작게 나온다 — 의도된 동작이다
- 깊이맵에는 파티클을 넣지 않는다 (거리값 오염 방지)

> 처음에는 부피를 대신하는 프록시 메시로 만들었다가 폐기했다. 라벨은 항상 있는
> 프록시에서, 이미지는 매번 달라지는 파티클에서 나오니 **둘이 따로 놀았다** —
> 이미지에 연기가 없는데 라벨만 남는 프레임이 생겼다(박스를 이미지에 얹어 확인).

## 궤적 예측 모델도 꽂을 수 있다

계획서의 예측 3단계는 `등속 → 칼만 → 학습형`이다. 앞의 둘은 시뮬레이터가 직접 푼다.
**학습형은 팀원 모델**이므로 꽂을 자리만 만들어 뒀다 — 검출기와 같은 방식이다.

📊 데이터 탭 → *궤적 예측* → `학습형` 선택 후, 콘솔에서 함수 하나를 등록한다.

```javascript
window.__customPredictor = async (obs) => {
  // obs = { now, pos:{x,z}, vel:{x,z}, hist:[{t,x,z},…], horizon, mPerAU }
  //   pos·vel·hist : 씬 좌표(AU) — 미터로 바꾸려면 mPerAU를 곱한다
  //   hist         : 최근 2초 관측, 오래된 것부터
  return {
    path: [{t:0, x:…, z:…}, …, {t:obs.horizon, x:…, z:…}],  // t = 지금부터 초
    sigma: 0.2,   // 지평선 끝 위치 불확실성 1σ (m)
  };
};
```

| 항목 | 동작 |
|---|---|
| 호출 주기 | 10Hz (`__sim.CPRED.hz`로 변경) |
| 응답 대기 중 | 직전 경로를 계속 사용 — 실제 온디바이스 추론과 같은 조건 |
| **1초 넘게 응답 없음** | **칼만으로 자동 대체** (멈춘 모델의 옛 경로로 판정하면 위험) |
| 예외 발생 | 칼만으로 대체 + 패널에 실패 횟수 표시 |
| `sigma` 생략 | 0으로 간주 — **보수적이지 않으니 반드시 채울 것** |

`sigma`는 안전 판정에 직접 들어간다. 지평선 끝에서 `sigma`만큼 **더 가까이 있을 수 있다**고
보고 정지/감속 시점을 앞당긴다. 칼만은 이 값을 스스로 내지만(실측 σ 0.47m), 학습형은
모델이 직접 내야 한다.

## 치수 — 실물 대조로 뒷받침됨 (2026-08-18)

씬은 **"튀김솥 외경 = 1.25 m"** 가정 위에 서 있다. 이 값 자체는 여전히 실측이 아니지만,
**서로 독립적인 실물 기준 3개가 씬 안에서 서로 어긋나지 않는다**는 것을 확인했다:

| 대상 | 시뮬 | 실물 | |
|---|---|---|---|
| 로봇 도달거리 | 1.70m | 두산 **H2017 = 1,700mm** | ✅ |
| 솥 작업 높이(림) | 0.905m | 회전식 국솥 **850~920mm** | ✅ |
| 솥 림 ÷ 사람 키 | 52.6% | 900 ÷ 1620 = 55.6% | ✅ |

특히 **림 높이가 실물 국솥 규격과 맞는다**는 게 스케일을 뒷받침한다. 솥 지름 1.25m는
순수 솥(Ø0.92~1.12m)보다 크고 회전 프레임 포함 폭(1.40~1.70m)보다 작아 그 사이 값으로
무리가 없다.

> 그래도 **솥 외경 실측 1건은 여전히 받아야 한다.** 위는 "어긋나지 않는다"는 확인이지
> "맞다"는 증명이 아니다. 미터로 인용하는 값(로봇 도달거리, 안전거리, GT 깊이)은
> 그 실측이 들어오기 전까지 오차 여지를 안고 있다.

실측 1건이 생기면 지오메트리를 다시 만들 필요 없이 파라미터 하나로 전체가 재보정된다.

```
sim.html?fryer=1.18
```

이때 데이터셋 `gt/*_meta.json` 의 `scale.verified` 가 `true`로 바뀌고 깊이 범위도 함께
환산된다. 재는 방법은 `analysis/measurement_request.md`.

## 런타임 파일

`vendor/ort/` — onnxruntime-web 1.20.1 (WebGPU JSEP 포함).
프로젝트에 동봉돼 있어 **실행 중 외부 네트워크 접근이 없다**.

```
ort.webgpu.min.js                  326KB
ort-wasm-simd-threaded.jsep.wasm    21MB   (WebGPU/JSEP)
ort-wasm-simd-threaded.wasm         11MB   (wasm 폴백)
```

## 알려진 제약

- `wasmPaths`는 **절대 URL**이어야 한다 (상대경로면 ES 모듈 해석 실패).
  `file://` 더블클릭이 아니라 `python -m http.server`로 띄워야 동작한다.
- 스레드 수는 1로 고정 — COOP/COEP 헤더 없이 동작시키기 위함.
  성능이 부족하면 서버에 해당 헤더를 추가하고 `__sim.ONNX.ort.env.wasm.numThreads` 를 올린다.

---

## 팀원에게 보낼 것 — 최소 배포 세트 (51MB)

아래만 복사해서 압축하면 각자 로컬에서 자기 모델로 돌려볼 수 있다.
**실측 검증(2026-08-13)**: 깨끗한 폴더에 아래만 복사해 다른 포트로 띄웠을 때
메시 1027개·설비 21개·사람·물리·로봇 도달성 정상, ONNX 추론 WebGPU 79.5ms·박스 3개, 콘솔 에러 0.

```
START_HERE.md  MODEL_HANDOFF.md             ← 설명서. 빠뜨리면 팀원이 아무것도 모른다
sim.html  index.html                        264KB
babylon.js  babylonjs.loaders.min.js        8.3MB
HavokPhysics_umd.js  HavokPhysics.wasm.js   2.8MB
assets/   (실제 로드되는 GLB 20개만)          6.5MB
vendor/ort/                                  32MB
```

`assets/` 는 sim.html이 실제로 부르는 20개만 넣는다. 목록:

```
basket  kitchen_static  lid  cook_rig_v47
robot_j1  robot_upper  robot_fore  robot_hand  robot_pedestal
env_table  env_rack  env_sink  env_bin  env_pancart  env_wallrack
env_kettle_nb  env_kettle_nb_closed  env_fridge  env_serve  env_basketcart
```

`assets/_legacy`, `cook_cc_v1.glb`, `sketchfab_cook_raw.glb` 등 **작업 부산물 220MB는 제외**한다.

> **`refs/` 는 배포하지 않는다.** 실제 급식실 CCTV 프레임이고 조리원이 찍혀 있다.
> 참조 오버레이(`sim.html?ref=…`)는 이 폴더가 없으면 조용히 꺼지며, 시뮬레이터 동작에는
> 영향이 없다. 같은 이유로 `analysis/참조 현장NDEX_INTERNAL.md`(학교명 대응표)도 제외한다.

### 실행

```bash
python -m http.server 5173
```
→ `http://localhost:5173/sim.html`

### 반드시 지켜야 할 것

| | 이유 |
|---|---|
| **`file://` 더블클릭 금지** | ORT가 wasm/mjs를 ES 모듈로 로드해 실패한다. 반드시 http 서버로 |
| **WebGPU 지원 브라우저** | Chrome/Edge 113+ 권장. 없으면 wasm으로 자동 폴백(느림) |
| **런타임 네트워크 불필요** | 외부 CDN 참조 0건 — 인터넷 없이 동작 |
